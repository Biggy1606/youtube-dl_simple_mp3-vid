# Auto-detect text files and normalize line endings
* text=auto

# Force LF for shell scripts (required for Linux/macOS)
*.sh text eol=lf

# Force CRLF for Windows-only scripts
*.bat text eol=crlf
*.ps1 text eol=crlf
